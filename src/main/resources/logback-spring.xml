<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="pai-score" />

    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                          "service":"${appName}",
                          "level":"%level",
                          "logger":"%logger",
                          "thread":"%thread",
                          "message":"%message",
                          "trace_id":"%X{trace_id}",
                          "traceId":"%X{traceId}",
                          "requestId":"%X{requestId}",
                          "userId":"%X{userId}",
                          "roomCode":"%X{roomCode}",
                          "event":"%X{event}",
                          "method":"%X{method}",
                          "path":"%X{path}",
                          "status":"%X{status}",
                          "durationMs":"%X{durationMs}",
                          "clientIp":"%X{clientIp}"
                        }
                    </pattern>
                </pattern>
                <stackTrace>
                    <fieldName>exception</fieldName>
                </stackTrace>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE_JSON" />
    </root>
</configuration>
